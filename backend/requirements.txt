fastapi
uvicorn[standard]
instagrapi
opencv-python
numpy
Pillow
ImageHash

# Face engine: DeepFace (TensorFlow) — current default
deepface
tf-keras

# Face engine: InsightFace path — runs entirely on onnxruntime (no TensorFlow)
insightface
onnxruntime
hsemotion-onnx

# Test harness (dev only — not needed at runtime)
pytest
pytest-cov
httpx
